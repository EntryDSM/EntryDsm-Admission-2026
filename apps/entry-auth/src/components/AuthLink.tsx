import styled from "@emotion/styled";
import { colors } from "@entry/design";

export const AuthLink = styled.div`
  width: 130px;
  display: flex;
  justify-content: center;
  border-inline-start: 2px solid ${colors.gray[100]};
  cursor: pointer;
`;

export const AuthLinkText = styled.div`
  cursor: pointer;
`;
